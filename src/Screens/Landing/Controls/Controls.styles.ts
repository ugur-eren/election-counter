import {StyleSheet} from 'react-native';
import {Spacing} from '../../../Styles';

export default StyleSheet.create({
  container: {
    gap: Spacing.normal,
  },
  counts: {
    flex: 1,
  },
  buttons: {
    gap: Spacing.xsmall,
  },
});
